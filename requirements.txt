# ===================================
# 数学教学智能体 — Python 依赖
# 安装：pip install -r requirements.txt
# ===================================

# --- Web 框架 ---
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# --- 数据库 ---
sqlalchemy>=2.0.0
aiosqlite>=0.19.0
asyncpg>=0.29.0
pymysql>=1.0.0

# --- 认证与安全 ---
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0  # 必须：密码哈希，无 bcrypt 时不会降级到 SHA-256

# --- HTTP 客户端 ---
httpx>=0.27.0

# --- 配置与校验 ---
pydantic>=2.0.0
pydantic-settings>=2.0.0

# --- 图片处理 ---
pillow>=10.0.0

# --- 速率限制 ---
slowapi>=0.1.9  # 可选：登录/注册接口速率限制

# --- 可选：OCR 文字提取（安装后自动启用） ---
# paddleocr>=2.6.0
# paddlepaddle>=2.4.0

# --- 开发 / 测试 ---
ruff>=0.3.0
pytest>=7.0.0
pytest-asyncio>=0.21.0